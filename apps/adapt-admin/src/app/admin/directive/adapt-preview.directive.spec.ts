import { AdaptPreviewDirective } from './adapt-preview.directive';

describe('AdaptPreviewDirective', () => {
  it('should create an instance', () => {
    const directive = new AdaptPreviewDirective();
    expect(directive).toBeTruthy();
  });
});
