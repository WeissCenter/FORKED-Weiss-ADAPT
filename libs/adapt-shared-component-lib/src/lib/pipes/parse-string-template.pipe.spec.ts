import { ParseStringTemplatePipe } from './parse-string-template.pipe';

describe('ParseStringTemplatePipe', () => {
  it('create an instance', () => {
    const pipe = new ParseStringTemplatePipe();
    expect(pipe).toBeTruthy();
  });
});
