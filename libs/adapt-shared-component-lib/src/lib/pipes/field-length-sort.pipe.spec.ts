import { FieldLengthSortPipe } from './field-length-sort.pipe';

describe('FieldLengthSortPipe', () => {
  it('create an instance', () => {
    const pipe = new FieldLengthSortPipe();
    expect(pipe).toBeTruthy();
  });
});
